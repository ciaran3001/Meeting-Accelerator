App As appinfo:
    BackEnabled: =false
    OnStart: |-
        =Set(SelectedMeetingTab, "Today");
        
        ClearCollect(SettingNavigationItems, {label: "Preferences"}, {label : "Personas"}, {label : "Meeting Types"});
        
        ClearCollect(SideNavItems, 
        {Name: "Home", UnselectedIcon : Home, SelectedIcon : 'Home-white' },
        {Name: "Analyse", UnselectedIcon : Analyse, SelectedIcon : 'Analyse-White' },
        {Name: "Settings", UnselectedIcon : Cog, SelectedIcon : 'Cog-White' }
        );

    Teams As hostControl.TeamsHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

